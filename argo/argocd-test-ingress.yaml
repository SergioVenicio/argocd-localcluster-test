kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: go-hello
spec:
  rules:
  - host: 127.0.0.1:8080
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
             name: go-hello
             port:
               number: 8080